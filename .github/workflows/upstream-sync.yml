name: Weekly Upstream Sync
on:
  schedule:
    - cron: '0 6 * * 0'
  workflow_dispatch:
permissions:
  contents: write
  workflows: write
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - run: echo "test"
