name: Weekly Upstream Sync & Database Upload

on:
  schedule:
    - cron: '0 6 * * 0'
  workflow_dispatch:
    inputs:
      skip_render_deploy:
        description: 'Skip Render redeploy (for testing)'
        required: false
        default: 'false'
        type: boolean

permissions:
  contents: write

jobs:
  sync-build-upload:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Sync from upstream
        run: |
          echo "📥 Adding upstream remote..."
          git remote add upstream https://github.com/czlonkowski/n8n-mcp.git || true

          echo "📥 Fetching upstream..."
          git fetch upstream

          echo "🔀 Merging upstream/main..."
          git merge upstream/main --no-edit -X ours || {
            echo "⚠️ Merge conflict detected, resolving..."

            git checkout --theirs package*.json || true
            git checkout --theirs src/ || true
            git checkout --theirs tsconfig.json || true
            git checkout --ours dist/ || true
            git checkout --ours patches/ || true
            git checkout --ours .github/workflows/weekly-sync.yml || true

            git add .
            git commit -m "chore: auto-resolve merge conflicts"
          }

          git push origin main

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Clone n8n docs
        run: git clone --depth 1 https://github.com/n8n-io/n8n-docs.git n8n-docs

      - name: Build TypeScript
        run: npm run build

      - name: Build MCP database
        run: npm run rebuild

      - name: Fetch workflow templates
        env:
          MIN_VIEWS: 0
          DATE_MONTHS: 0
        run: npm run fetch:templates

      - name: Validate database
        run: |
          echo "🔍 Validating database..."
          TEMPLATE_COUNT=$(sqlite3 data/nodes.db "SELECT COUNT(*) FROM workflow_templates;")
          echo "Template count: $TEMPLATE_COUNT"

          if [ "$TEMPLATE_COUNT" -lt 100 ]; then
            echo "❌ Database validation failed: only $TEMPLATE_COUNT templates"
            exit 1
          fi

          echo "✅ Database validated: $TEMPLATE_COUNT templates"

      - name: Compress database
        run: gzip -c data/nodes.db > nodes.db.gz

      - name: Upload to Supabase Storage
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: |
          echo "📤 Uploading to Supabase..."

          curl -X PUT "${SUPABASE_URL}/storage/v1/object/build-cache/nodes.db.gz" \
            -H "Authorization: Bearer ${SUPABASE_SERVICE_KEY}" \
            -H "Content-Type: application/gzip" \
            -H "x-upsert: true" \
            --data-binary @nodes.db.gz \
            --fail

          if [ $? -eq 0 ]; then
            echo "✅ Upload successful"
          else
            echo "❌ Upload failed"
            exit 1
          fi

      - name: Trigger Render redeploy
        if: ${{ github.event.inputs.skip_render_deploy != 'true' }}
        env:
          RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
          SERVICE_ID: ${{ secrets.SERVICE_ID }}
        run: |
          echo "🔄 Triggering Tech Factory redeploy..."

          curl -X POST "https://api.render.com/v1/services/${SERVICE_ID}/deploys" \
            -H "Authorization: Bearer ${RENDER_API_KEY}" \
            -H "Content-Type: application/json" \
            -d '{"clearCache": "do_not_clear"}' \
            --fail-with-body

          if [ $? -eq 0 ]; then
            echo "✅ Redeploy triggered successfully"
          else
            echo "❌ Redeploy trigger failed"
            exit 1
          fi
